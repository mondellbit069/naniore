#include<bits/stdc++.h>
using namespace std;
long long n,k,maxx,res,ans,dem,f[10000007];
int main(){
    freopen("KSEQ.inp","r",stdin);
    freopen("KSEQ.out","w",stdout);
    memset(f,0,sizeof(f));
    f[1]=1;
    for(long long i=1;i<=int(sqrt(10000007));i++)
    if(f[i]=0) 
            
    cin>>n>>k;
    long long a[n];
    for(long long i=0;i<n;i++){
        cin>>a[i];
    }
   
}