#include<bits/stdc++.h>
using namespace std;
long long n;
int main(){
    cin>>s;
    long long i=0;
    while(i<=s.length() && s[i]=='4' || s[i]=='7') i++;
    if(i=s.length())
    cout<<"YES";
    else cout<<"NO";
}